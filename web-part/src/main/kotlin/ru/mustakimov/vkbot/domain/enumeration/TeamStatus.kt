package ru.mustakimov.vkbot.domain.enumeration

/**
 * The TeamStatus enumeration.
 */
enum class TeamStatus {
    ADDED, CASE_SELECTION, CASE_SELECTED, REGISTRATION, REGISTERED, PARTICIPANTS_NEEDED, CANCELED
}
